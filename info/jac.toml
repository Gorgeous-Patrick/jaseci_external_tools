[project]
name = "info"
version = "0.1.0"
description = "Command-line Jac project"
entry-point = "main.jac"
kind = "cli"
jac-version = "==0.35.0"

[dependencies]

[dev-dependencies]
watchdog = ">=3.0.0"
